# Capturas de tela do manual SST

Imagens em `public/adms/manual/img/sst/`.

No HTML do manual: `{{IMG:dashboard}}` resolve para `dashboard.png` (se existir) ou `dashboard.svg`.

Salve PNGs reais com os mesmos nomes dos SVG para substituir automaticamente.

Recriar placeholders: `php scripts/ensure_manual_screenshot_placeholders.php`
